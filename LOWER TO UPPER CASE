#include<stdio.h>
#include<string.h>
main()
{
    int i;
    char str[20];
    printf("enter strings:");
    gets(str);
    while(str[i]!='\0')
     {
         if(str[i]>='a'&&str[i]<='z')
         str[i]=str[i]-32;
         i++;
     }
     puts(str);
 }
