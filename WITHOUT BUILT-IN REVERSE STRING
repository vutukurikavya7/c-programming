#include<stdio.h>
#include<string.h>
main()
{
    int i=0,j;
    char str[20],c;
    printf("enter the string\n");
    gets(str);
   j=strlen(str)-1;
    while(i<j)
    {
        c=str[i];
        str[i]=str[j];
        str[j]=c;
        i++;
        j--;
    }
    puts(str);
}
