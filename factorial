#include<stdio.h>
main()
{
    int n,i,f=1;
    printf("enter value of n");
    scanf("%d",&n);
    printf("the factorial of %d is\n",n);
    for(i=1;i<=n;i++)
    {
        f=f*i;
    }
printf("%d\n",f);
    
}
