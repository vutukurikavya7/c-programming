#include<stdio.h>
int fib(int n);
main()
{
    int n,c;
    printf("enter value of n");
    scanf("%d",&n);
   c=fib(n);
   printf("%d ",c); 
}
int fib(int n)
{
    if(n==1)
    return 0;
    else if(n==2)
    return 1;
    else
    return fib(n-1)+fib(n-2);
    
    
}
