#include<stdio.h>
main()
{
    int n,i,r;
    printf("enter value of n\n");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        for(r=1;r<=i;r++)
        {
            printf("%d",r);
        }
 printf("\n");
    }
}

