#include<stdio.h>
#include<string.h>
main()
{
    int i=0,j,l;
    char str[20],temp[20];
    printf("enter string:");
    gets(str);
    l=strlen(str);
    j=l-1;
    i=0;
    while(str[j]!='\0'&&j>=0)
    {
temp[i]=str[j];
    i++;
    j--;
    }
    temp[i]='\0';
    puts(temp);
    

}
