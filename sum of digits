#include<stdio.h>
main()
{
    int n,i,d,sum;
    printf("enter value of n");
    scanf("%d",&n);
    sum=0;
   while(n!=0)
    {
    for(i=1;i<=n;i++)
    {
        d=n%10;
        n=n/10;
        sum=sum+d;
        
    }
}
printf("%d",sum);
    
}
